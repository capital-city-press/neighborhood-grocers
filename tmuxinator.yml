# ~/.tmuxinator/neighborhood-grocers.yml

name: neighborhood-grocers
root: ~/projects/http/neighborhood-grocers

windows:
  - command: git status -s
  - editor: vim .
  - build: gulp watch
  - build: ruby -run -e httpd . -p 5000
